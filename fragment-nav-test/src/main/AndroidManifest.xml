<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jskierbi.fragment_nav">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ActivityWithToolbarAndDrawer"
            android:label="FNC Test">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".StateSavingActivity" />
        <activity android:name=".ActivityWithToolbarAndDrawerAlways" />
        <activity android:name=".ActivityWithToolbarAndDrawerNavRoot" />

    </application>

</manifest>
